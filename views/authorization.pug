html
  head
    title= title
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    div(class='page-header')
       h1(class='registration') Авторизация
    div 

    form(action="", method="POST")
        div.form-group 
            label(for='login') Ваш логин:
            input#login.form-control(type='text', placeholder='Login' name='login' value=(undefined===login ? '' : GUEST.login), required='true')

            label(for='login') Ваш пароль:
            input#pass.form-control(type='text', placeholder='Password' name='pass' value=(undefined===pass ? '' : GUEST.pass), required='true')

            button.btn.btn-primary(type='submit') Войти

        if errors 
            ul 
                for error in errors 
                    li!= error.msg 